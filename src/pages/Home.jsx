import React from "react";

// front end authenticate inputs
// make pet / list of pets components
function Home() {
  // async function getUserPets(id) {
  //   const response = await axios.get(`http://localhost:5000/pet/user/${id}`);
  //   setUser(response.data);
  //   // user , user.cart , savedPets
  //   console.log("cart and owned", response.data);
  // }
  // useEffect(() => {
  //   getUserPets(user._id);
  // }, []);
  // search

  return <div></div>;
}

export default Home;
